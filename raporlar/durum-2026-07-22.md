# Orion Aethelred — Proje Durum Raporu
**Tarih:** 2026-07-22  
**Commit:** cae5604 (başlangıç) → bu oturumda güncellendi  
**Test Durumu:** 498/498 ✅ (önceki oturum: 493/498, 5 failure)

---

## Test İyileştirmeleri (Bu Oturum)

| Test | Önceki | Sonrası | Neden |
|------|--------|---------|-------|
| `detectKittyProtocol` (×4) | FAIL | PASS | `detectTerminalProtocol` olarak yeniden adlandırılmıştı; testler güncellenmemişti. Return tipi `true/false` → `"kitty"/"iterm2"/null` |
| `belirli ref için çalışır` (git_show) | FAIL | PASS | `HEAD~1` büyük TS migration commit'i; `spawnSync` 2MB buffer taştı (ENOBUFS). ENOBUFS'ta `--stat`-only fallback eklendi |

---

## Düzeltilen Buglar

| Dosya | Bug | Düzeltme |
|-------|-----|----------|
| `core/selfdev.ts:59` | `restart()` fonksiyonu `orion.js` spawn ediyordu; dosya `orion.ts`'e taşındığından fail ediyordu | `orion.js` → `--experimental-strip-types orion.ts` |
| `core/session-dispatch.ts:64` | `return undefined` — tüm backend'ler denendikten sonra erişilemeyen dead code. Caller `undefined`'ı metin olarak kullanabilirdi | `throw new Error("unreachable: ...")` |
| `tools/git.ts` `git_show` | `spawnSync` ENOBUFS üzerine exception fırlatıyordu | try/catch + `--stat`-only fallback |
| `tools/git.ts` `_run()` | `maxBuffer` parametrize edilmemiş, her komut 2MB ile sınırlıydı | `maxBuffer` parametresi eklendi |

---

## Temizlenen Dosyalar

| Dosya/Dizin | İşlem | Neden |
|-------------|-------|-------|
| `beta3-test-output.txt` (273KB) | **Silindi** | Binary PNG + terminal escape sequence içeriyor, kaynak kontrolünde anlamsız |
| `core/agents/KANIT_image.md` | `docs/` altına **taşındı** | `.ts` kaynak dizininde `.md` dosya yanlış yer |
| `core/agents/KANIT_voice.md` | `docs/` altına **taşındı** | Aynı sebep |
| `.gitignore` | Güncellendi | `kitty/`, `tui/beta2/`, `tui/beta3/`, `*-test-output.txt` eklendi |

---

## Yeni Dosyalar

| Dosya | Açıklama |
|-------|----------|
| `~/.claude/skills/tayf-router/SKILL.md` | TAYF yönlendirme mantığını Claude Code skill formatında tanımlar. `/tayf` komutuyla tetiklenir |
| `raporlar/durum-2026-07-22.md` | Bu dosya |

---

## Genel Proje Durumu

### Mimari

```
orion.ts (CLI/TUI)    orion-server.ts (HTTP+SSE)    orion-mcp.ts (MCP)
        │                      │                            │
        └────────── core/session.ts (456 satır, 5 modüle split) ──┘
                               │
         ┌─────────────────────┼──────────────────────────┐
    backends/            core/router.ts              core/tools.ts
    (9 backend)          Thompson sampling             (31 araç)
                         FEP shadow mode
```

### TypeScript Durumu

- **%100 TypeScript** — `.js` kaynak dosya sıfır (tui/animation, electron, scripts hariç — kasıtlı izole)
- `allowJs: false`, `checkJs: false`, `noImplicitAny: false`, `strict: false`
- `// @ts-nocheck` ile hızlı geçiş, kademeli tip güçlendirmesi hedefi açık
- `npm run typecheck` → 0 hata

### Test Coverage

- **498 test, 48 suite** — `npm test` 0 fail
- **Coverage açıkları** (öncelik sırasına göre):
  - `core/session-dispatch.ts` — fallback zinciri test edilmiyor
  - `core/session-compact.ts`, `core/session-memory.ts`, `core/session-system.ts` — session split modülleri
  - `core/daemon.ts` — vault daemon
  - `core/mcp.ts` — MCP sunucu bağlantısı
  - `backends/nim.ts`, `backends/openrouter.ts` — izole testleri yok

### CodeGraph

- `.codegraph/` — 1032 dosya, 28.769 node, 88.475 edge
- MCP: `codegraph_explore` aktif (Claude Code global config'de)

---

## Açık Sorunlar (Düşük Öncelik)

| # | Sorun | Dosya | Önem |
|---|-------|-------|------|
| 1 | `core/skills-registry.ts` — `_cache` invalidasyonu yok; yeni agent eklendikten sonra cache eski kalır | `skills-registry.ts:12` | Orta |
| 2 | `core/diagnostics.ts:42` — `tsc --noEmit` tek dosya için proje bağlamı olmadan çalışıyor; `.ts` dosyaları `checkNode`'a düşebilir | `diagnostics.ts` | Orta |
| 3 | `tui/animation/` — `controller.js` `.js` uzantısıyla require ediliyor; ana projedeki `.ts` standardına aykırı. İzole sub-proje, kasıtlı ama belgelenmemiş | `orion.ts:28` | Düşük |
| 4 | `tui/beta2/` — hiçbir üretim kodu bağlamıyor, test dizinleri de ona bağlı değil. Terk edilmiş UI prototipi | `tui/beta2/` | Düşük |
| 5 | SIGWINCH Windows'ta çalışmıyor (terminal resize tepkisi) | `tui/index.ts` | Düşük |
| 6 | Thompson sampling: `vibethinker`, `ornith` özel format → `noToolSupport` bayrağı otomatik set edilmeli | `core/thompson.ts` | Düşük |

---

## TAYF Durumu

TAYF v0.1 belgesi (`TAYF/TAYF_v0.1.md`) damıtım bekleniyor:
- Meissa'nın batch koşuları tamamlandıysa: `node scripts/tayf_distill.js`
- Level 0 trigger paleti gerçek verilerle güncellenmeli (`core/agents/level0.ts`)
- `tests/level0.test.js` (38 test) geçiyor ✅

Yeni ekleme: `tayf-router` skill (`~/.claude/skills/tayf-router/SKILL.md`) — TAYF mantığını Claude Code oturumunda çalıştıran yönlendirici. Trigger: `/tayf`

---

## Dizin Yapısı (Temizlenmiş)

```
molp/
├── orion.ts              — CLI entry point
├── orion-mcp.ts          — MCP sunucu
├── orion-server.ts       — HTTP+SSE sunucu
├── CLAUDE.md             — Orion kimlik/kurallar
├── PERSONA.md            — Ses karakteri
├── AGENTS.md             — Multi-agent rehber
├── merak.md              — Orion & Ozyn merak listesi
├── gozlemler.md          — Platform gözlemleri
├── backends/             — 9 backend (anthropic, ollama, openai-compat, ...)
├── core/                 — Çekirdek modüller
│   ├── agents/           — Meissa, Level0, imager, voice (sadece .ts)
│   ├── commands/         — 32 CLI komutu
│   └── loops/            — Anthropic, OpenAI, Ollama loop'ları
├── tools/                — git, fs araçları
├── tui/                  — TUI bileşenleri (.ts)
│   └── animation/        — İzole 3D animasyon sub-projesi (.js, kasıtlı)
├── tests/                — 48 suite, 498 test
├── scripts/              — Meissa batch, TAYF distill, Moltbook PS1
├── docs/                 — Mimari belgeler + KANIT dosyaları
├── TAYF/                 — TAYF protokol belgeleri
├── prototipdocs/         — Araştırma arşivi (UI testleri, teori)
├── raporlar/             — Durum raporları (bu dizin)
├── electron/             — Masaüstü uygulaması (izole)
└── logs/                 — Çalışma zamanı logları (gitignored)
```

**Gitignored (kasıtlı):**
- `kitty/` — Kitty terminal kaynak kodu (geliştirme referansı)
- `tui/beta2/`, `tui/beta3/` — Terk edilmiş TUI prototipleri
- `graphify-out/` — CodeGraph analiz çıktıları
- `credentials.json`, `.mcp.json`, `logs/`
